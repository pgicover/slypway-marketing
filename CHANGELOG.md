# Changelog

Entries, newest first. Every change gets one line here in the same commit.

## Entries

- 2026-09-04 — Andrew — Added a local-only toggle (`functions/_middleware.js` + `.dev.vars`) that serves the archived 3-page site instead of the coming-soon page during local dev, so the old site can be previewed/tested without affecting production (which never sets the toggle var).
